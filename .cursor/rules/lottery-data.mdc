---
description: 处理 data/ 下原始与清洗数据时的校验与版本约定
globs: data/**/*
alwaysApply: false
---

# 数据文件工作规则

- **不得**在无备份情况下覆盖唯一原始抓取文件；新增期次用追加或新文件并记录来源与时间。
- 每期记录至少包含：彩种、期号、全部开奖号码（及大乐透后区、双色球蓝球等分区字段）。`data/processed/` 下规范化 CSV 可不存开奖日期列，以 `period_id` + 号码列为分析与预测主键；若业务需要日历信息，可保留在 `data/raw/` 附件或 `manifest.json` 的备注字段中。  
- **排列5字段口径（新增）**：`pl5_draws.csv` 使用 `lottery_type,period_id,d1,d2,d3,d4,d5`；其中 `d1`–`d5` 均为 **0–9** 整数，**允许重复数字**（与 DLT/SSQ/KL8 的互异规则不同）。
- 导入后执行一致性检查：号码区间、去重、期号单调、与公开来源抽样比对。
- 若发现冲突，以用户指定的「权威来源」为准并记录决策日志（可用 `data/processed/CHANGELOG.md` 等形式，由实现阶段补充）。

## 单一事实源（推荐）

- **`data/processed/`**：经 `lottery-draw-sync` / `lottery-draw-dlt-ssq` 校验合并后的 **规范化主数据**（CSV + `manifest.json`）。分析与预测 Agent **优先**读取此处；若不存在则回退到 `data/raw/` 或用户提供的其它可追溯文件，并在输出中写明数据来源层级（本仓库不约定根目录 `data/*.xlsx`）。
- **`data/raw/`**：原始抓取副本，允许保留多版本；不作为长期唯一分析入口。
