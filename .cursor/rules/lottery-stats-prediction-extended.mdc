---
description: 扩展的统计分析维度与统计型预测参考口径（大乐透/双色球/快乐八/排列5）
alwaysApply: true
---

# 扩展：统计分析 与 统计型预测

本规则在 `lottery-core` 合规与 Agent 职责隔离前提下，约定**允许且鼓励**的额外分析/参考类型。执行主体不变：**描述性统计与数据质量** → `lottery-history-analysis`；**基于明确口径的号码/结构参考** → `lottery-prediction`。

## AC 值（算术复杂度，本仓库统一口径）

采用国内数字彩讨论中**较常见**的「算术复杂度」定义（与多数走势/分析软件一致思路）：

1. 将当期参与计算的一组号码按从小到大排序：x1 < x2 < … < xn。  
2. 列出所有两两之差的绝对值 |xj − xi|（i < j），共 n×(n−1)/2 个。  
3. 设 **D** 为上述差值中去重后的**不同正差值个数**。  
4. 定义 **AC = D − (n − 1)**。

**适用（本仓库默认）**

| 彩种 | 计算对象 | n | 公式 |
|------|-----------|---|------|
| 双色球 | **红球 6 枚**（仅红球，不含蓝球） | 6 | AC = D − 5 |
| 大乐透 | **前区 5 枚**（仅前区，不含后区） | 5 | AC = D − 4 |

**不适用**：双色球蓝球仅 1 枚、大乐透后区仅 2 枚（n < 3）——**不计算 AC**；若文中提及须写「该组号码个数不足，不适用 AC」。  
**快乐八**：每期 20 个开奖号若用户**明确要求** AC，可用同一公式 n = 20、AC = D − 19，须在报告中标注「非主流用法，仅作探索性描述」。

涉及 AC 的分析或预测输出中，**至少首次出现处**用一两句话复述上表（可简写「算术复杂度：不同两两差值个数减 (n−1)」），避免与少数其他定义混淆。

## 一、扩展统计分析（`lottery-history-analysis`）

在既有频次、遗漏、奇偶、和值区间、共现、数据质量之外，可包含：

1. **时间维度聚合**：按自然月/年或滑动窗口，对每期衍生指标做均值、分位数、计数（须写清窗口定义）；可与「全样本」对照说明季节性仅为描述。
2. **期级衍生指标**（按彩种适用性选用，定义须写进报告）：  
   - **大乐透 / 双色球**：前区/红球 **和值、跨度、连号个数、AC 值**（定义见本节 **「AC 值」**）、质合比等。  
   - **双色球**：蓝球单独频次与遗漏。  
   - **快乐八**：每期 **20 个开奖号** 的 **和值、奇偶比、上下区比（如 01–40 vs 41–80）**、号码间距分布等；若联系选十玩法须单列说明视角。
3. **多窗口对照**：同一指标在 N₁、N₂、N₃ 期（如 30/100/300）下的对比表，仅作稳定性描述，**不作**「未来走势」结论。
4. **跨彩种对比**（可选）：仅允许表格化 **并列描述**（期均和值、奇偶结构等），禁止推断因果或「联动开奖」。
5. **与简单随机基准对照**（可选）：在**明确假设**（如号码独立均匀等）下，用解析或模拟得到某统计量的参考分布，对比观测分位；须声明假设与真实摇奖的差异，**禁止**包装为显著性「预测信号」或 p 值即中奖依据。

## 二、扩展统计型预测参考（`lottery-prediction`）

在既有频次、遗漏、结构类、快乐八选十说明之外，可包含：

1. **多窗口 N 对比**：同一指标在近 N₁ vs N₂ vs N₃ 的结论差异表；须说明窗口选择理由与结论**不可外推**。
2. **分位与区间**：和值、跨度等落在预设区间的历史频率；输出为「历史上常落区间」的**频率叙述**，不得改写为「下期必落某区间」。
3. **用户约束下的频次表**：在用户给定禁号、必选号、奇偶偏好等**硬约束**下，对剩余号码或合法组合空间做**计数/频次**叙述；仍为统计参考，须声明随机性。
4. **组合结构参考**（非组号方案）：如红球奇偶比、大小比的历史频率，**不**与 `lottery-combo-optimize` 的投注单混写；若用户同时要组号，移交组合 Agent。

## 三、合规（叠加）

- 所有扩展输出须含：**彩种、期号范围、指标定义、数据路径**；预测类须含 **随机性与娱乐用途** 声明。  
- 禁止：稳赚话术、伪造回测收益、将 p 值或偏离度说成提高中奖概率。

## 四、职责边界（叠加）

- 扩展**分析**正文主体仍由 `lottery-history-analysis` 产出并更新 `history/*.md`。  
- 扩展**参考/预测口径**正文主体仍由 `lottery-prediction` 产出；不得用扩展条款绕过职责隔离（例如预测 Agent 仍不得替代完整 EDA 报告）。
