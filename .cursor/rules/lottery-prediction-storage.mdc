---
description: 统计型预测参考结果固定写入 history/ 下五个 Markdown，每次预测任务后由预测 Agent 更新对应文件
alwaysApply: true
---

# 预测参考结果落盘（`history/`）

## 固定文件与彩种对应

| 彩种 | 归档文件（相对仓库根） |
|------|------------------------|
| 大乐透 | `history/daletou_prediction.md` |
| 双色球 | `history/shuangseqiu_prediction.md` |
| 快乐八 | `history/kuaileba_prediction.md` |
| 排列5 | `history/pailie5_prediction.md` |
| 七星彩 | `history/qixingcai_prediction.md` |

## 更新义务

- **`lottery-prediction` Agent** 每完成一次该彩种（或含该彩种）的统计型预测参考任务，须在**同一轮任务内**更新上表中**涉及彩种**对应 Markdown：**写入本次完整输出**（可覆盖整文件），并更新文内「最后更新」「统计窗口（如近 N 期）」「期号范围」「所用数据路径」「是否引用分析归档路径」等元信息。
- **最新开奖重算强制**：每次预测任务前，必须先按 `data/processed/*.csv` 的最新 `period_id` 重新计算；输出前需保证预测文件内 `期号范围` 的末期等于对应 CSV 的最新期号（如 `dlt_draws.csv`、`ssq_draws.csv`、`kl8_draws.csv`、`pl5_draws.csv`、`qxc_draws.csv`），否则视为未完成本次任务。
- **analysis 同步刷新强制**：每次预测任务在输出前，必须同步更新对应彩种的 `history/*_analysis.md`（至少更新 `最后更新`、`统计窗口/期号范围` 并与最新 `period_id` 一致）。若 analysis 未刷新到同批次时间戳或最新期号，则预测任务视为未完成。
- **其他 Agent** 默认**不修改**上述五文件；仅可读取供参考，除非用户明确要求其编辑。

## 与历史分析归档的关系

- 预测归档可与 `history/*_analysis.md` 并列存在；**不得**用预测归档替代历史分析归档的职责。

## 文件内容建议结构

顶部元数据块 + 正文，**必须**包含以下章节（顺序可微调，但不得缺省「明确号码」）：

1. **口径说明**  
2. **结果摘要**（热冷、结构等统计叙述）  
3. **明确号码输出（强制）**：可复现规则下给出的**完整可见号码**——大乐透、双色球各 **5 注单式**（大乐透每注前 5 + 后 2；双色球每注红 6 + 蓝 1），且须写 **预测生成时间** 与 **每一号码的选择原因**；快乐八须含 **参考开奖 20 码（升序）** 与 **选十参考 11 码（升序）**；排列5须含 **5 注单式 5 位号码**（`d1`–`d5`，0-9，允许重复）及简要选号依据；七星彩须含 **5 注单式前 6 + 后 1**（`d1`–`d6` 为 0-9，`special` 为 0-14）及简要选号依据。  
   - **逐号原因强制（新增硬规则）**：预测归档中的所有“明确号码输出”必须对**每个号码位**给出可复查的选择原因，不得仅给整注概述。适用于大乐透（前5+后2）、双色球（红6+蓝1）、快乐八（20码与11码中的每个号码）、排列5（d1–d5）、七星彩（d1–d6+special）。
   - **快乐八重合上限（强制）**：预测得到的“参考开奖 20 码”与最新一期真实开奖 20 码的交集数量不得超过 **6**；若初稿超过 6，必须按同口径分数从低到高优先替换重合号，直至满足上限，并在正文标注“重合约束已执行（<=6）”。
   - **三彩种去核心化（强制）**：大乐透、双色球、快乐八均不得直接采用“最新窗口高核心组合”出号；若结果明显由最新窗口 Top 热号簇主导，必须执行降相关处理（如过热号惩罚、分区上限、热温冷混合抽取）后再定稿，并在正文写明“已执行去核心化约束”。
   - **大乐透/双色球防重合（强制）**：大乐透、双色球的每一注预测单式都必须通过双重校验：① 不得与历史任一期开奖完全一致；② 与最新一期开奖重合号码数必须 **<=3**。若不满足，必须替换低分重合号后再输出，并在正文标注“防重合约束已执行（历史不全重合 + 最新期<=3）”。
4. **单式优选（新增强制）**：在“明确号码输出”之后，必须额外给出 **1 注分数最高的单式**，其定义固定为：按同一评分口径对号码降序排序后，**直接取各球区前 N 个**组成一注（大乐透：前区前 5 + 后区前 2；双色球：红球前 6 + 蓝球前 1；快乐八选十参考：从候选集中取前 11 个用于 11 码复式；排列5：各位取 Top1；七星彩：前区 6 位各位取 Top1 + 后区 Top1）。若边界同分，使用固定 tie-break（如号码升序优先）或声明随机种子。并展示：`号码`、`总分`、`关键因子贡献`、`生成时间`。  
5. **使用说明 / 随机性声明**  
6. **附录：预算与投注推荐（脚本固写）**：`regenerate-history` 在 `history/daletou_prediction.md`、`history/shuangseqiu_prediction.md` 文末写入 **10～30 元（含）** 金额带规则，并说明正文 **5 注单式** 合计 **10 元**（无倍投）；在 `history/kuaileba_prediction.md` 文末为 **选十 11 码复式 ≈22 元**；在 `history/pailie5_prediction.md` 文末写入 **10～30 元带内** 的单式金额示例（默认 5 注共 10 元）。常量以 `src/lottery/config.py` 为准。更复杂方案仍由 **`lottery-combo-optimize`** 在金额带内优化并写校验与投注原因。

历史版本以 Git 为准；若用户要求多版保留，可在正文末追加日期分节。`python src/scripts/lottery.py regenerate-history`（`--only all`，以及 `--only kl8` / `--only pl5`）会按范围重写对应预测正文。快乐八预算说明见 `kuaileba_prediction.md` 文末提示与 `lottery-combo-optimize` 技能。

- **时间口径（强制）**：上述元数据中的 `最后更新` 与正文中的 `预测生成时间` 必须使用**北京时间**（ISO-8601，`+08:00`）。

## 默认窗口与脚本对齐

- 仓库约定：上述五彩种书面归档的**默认统计窗口为近 30 期**（期末尾连续段），与 `src/lottery/config.py` 中 `DEFAULT_STATS_WINDOW` 一致；**大乐透、双色球、快乐八、排列5、七星彩预测**均由 `regenerate-history`（按 `--only` 选择范围）按**同一套窗口规则**生成。Agent 手写覆盖归档时，元数据中的「统计窗口」须与正文计算口径一致；若用户指定其他 N，须全文一致标注。
