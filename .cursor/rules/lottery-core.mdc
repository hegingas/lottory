---
description: 彩票项目全局约束、合规表述、多 Agent 协作与职责隔离
alwaysApply: true
---

# 彩票项目核心规则

## 合规与表述

- 彩票为随机游戏，历史统计**不能**保证未来结果。禁止「稳赚」「必中」「投资回报率」等误导表述。
- 输出预测或推荐组合时，必须说明：**娱乐目的、概率本质、过往统计仅供参考**。
- 不协助未成年人购彩；不协助规避监管或伪造开奖数据。

## 彩种与范围

- **大乐透、双色球**：分析/优化须支持玩法描述维度上的 **单式、复式、胆拖**（实现时以官方规则为准计算注数与金额）。
- **快乐八**：分析/优化场景下 **仅选十**；组合优化场景中 **仅允许「选十 + 11 个号码复式」** 这一种复式形态，不得扩展到其他复式档位数除非用户明确要求。
- **七星彩**：前区 6 位（0–9，每位独立，允许重复）+ 后区 1 位（0–14）。按位匹配游戏，分析/预测参考排列5分位模型；组号以单式多注为主。
- **排列5**：分析的形制上亦为按位匹配（5 位数字，每位 0–9，允许重复）。

## 默认统计窗口（五彩种）

- **大乐透、双色球、快乐八、排列5、七星彩**：在未由用户或任务单另行指定窗口长度时，**统计分析**与**统计型预测参考**默认仅针对 **`data/processed/` 中已按期号排序后的期末尾连续 30 期**（与 `src/lottery/config.py` 中 `DEFAULT_STATS_WINDOW` 一致）。五彩种取数规则、窗口定义与书面归档结构保持一致；快乐八在「开奖 20 码」与「选十玩法」上的语义区分仍须遵守。
- **`regenerate-history`**（唯一推荐入口）：`--only all`（默认）刷新大乐透、双色球、排列5，并在存在 `kl8_draws.csv` 时追加快乐八、存在 `qxc_draws.csv` 时追加七星彩；`--only kl8` 仅快乐八；`--only dlt-ssq` 仅大乐透+双色球；`--only pl5` 仅排列5；`--only qxc` 仅七星彩。`regenerate-kl8-prediction` 为兼容别名，等同 `--only kl8`。
- 书面归档元数据须反映实际 N；若采用其他 N（如 50、100、全量），须在元数据与正文中**显式写明 N 及取数规则**，避免与默认混淆。
- **最新期强制重算（新增硬规则）**：每次收到「分析 / 预测 / 组号」请求时，必须先以 `data/processed/*.csv` 的**最新期号**为基准重算（或显式确认已是最新重算结果），禁止沿用旧期号缓存结果直接回复。若发现归档文件中的 `期号范围` 末期小于对应 CSV 最新 `period_id`，必须先重算并更新归档，再输出结论。
- **analysis 强制刷新（新增硬规则）**：每次执行“计算类任务”（至少含分析与预测）时，必须在同一轮任务内同步刷新对应 `history/*_analysis.md`；禁止只更新 `prediction` 不更新 `analysis`。若预测覆盖多个彩种，相关彩种 analysis 文件也必须全部更新到最新期号后，才可返回最终结论。
- **去核心化选号（新增硬规则）**：大乐透、双色球、快乐八在预测时，禁止使用“最新窗口高核心组合”直接出号（如直接围绕最近窗口 Top 热号骨架拼注）。必须采用去核心化口径：在同一综合评分框架内加入**反集中约束**（分区均衡、降相关或惩罚过热聚集），避免结果被单一热点簇主导。
- **大乐透/双色球重合约束（新增硬规则）**：大乐透、双色球预测结果必须满足两条：① 任一预测单式不得与历史任一期开奖**完全重合**；② 任一预测单式与各自最新一期开奖的重合号码数不得超过 **3**（按该彩种全号码位计数：大乐透前区+后区共 7 码、双色球红球+蓝球共 7 码）。
- **时间戳口径（新增硬规则）**：预测与分析归档中的 `最后更新`、`预测生成时间` 必须统一采用**北京时间**（ISO-8601，`+08:00`），禁止写 UTC 或省略时区。

## Agent 职责隔离（强制）

- **每个 Subagent 独立运行**：各自对话中只完成本角色职责，**禁止**输出或执行属于其他 Agent 的专属工作内容。
- **越界判定**：凡交付物的主体属于下表「专属」列，即只能由对应 Agent 产出；其他 Agent 至多 **转述需求、给出移交清单、引用已有路径**，不得代为完成。

| Agent `name` | 专属（仅该 Agent 做） | 禁止（不得由该 Agent 做） |
|--------------|----------------------|---------------------------|
| `lottery-manager` | 意图解析、数据文件盘点摘要、阶段排序、**移交下一步**的清单与建议提示词、跨阶段汇总（不含专精计算） | 历史描述性统计报表、冷热/常见号预测结论、采集/解析/落盘实现、投注单与注数优化方案 |
| `lottery-history-analysis` | 五彩种历史数据的描述性统计、分布、遗漏、数据质量报告（含七星彩、排列5） | 预测类号码推荐、采集与落盘、10～30 元组合方案、全流程编排 |
| `lottery-prediction` | 在**已声明口径**下的冷热号/常见号/结构倾向参考及随机性声明（含七星彩、排列5） | 完整历史 EDA 替代报告、采集与落盘、10～30 元组合方案、全流程编排（可要求用户提供数据路径或上一步分析摘要） |
| `lottery-draw-dlt-ssq` | **仅**大乐透与双色球：开奖数据采集/导入、解析校验、`data/processed` 中 `dlt_draws.csv` / `ssq_draws.csv` 与 manifest 相关条目更新 | 快乐八/七星彩/排列5同步、统计分析、预测推荐、投注组合、全流程编排 |
| `lottery-draw-sync` | **三彩种**（含快乐八）开奖数据抓取、解析、校验、落盘与溯源（当前不含排列5、七星彩） | 统计分析、预测推荐、投注组合、全流程编排 |
| `lottery-combo-optimize` | **10～30 元（默认）**内投注组合的注数/金额与方案（含七星彩、排列5） | 历史 EDA、预测推荐、采集与落盘、全流程编排 |

- 详细流程与数据基线见 `AGENTS.md`。

## 工程质量

- 数据来源需可溯源（官方公告、授权接口或用户提供的可追溯文件）；写入数据前做字段与期号校验。
- 金额与注数计算需可复查：给出公式或引用技能中的口径，避免口算错误。

## 扩展统计与预测口径

- 允许的**额外**分析维度与统计型预测参考类型见 `.cursor/rules/lottery-stats-prediction-extended.mdc`；与 `lottery-history-analysis`、`lottery-prediction` 技能正文一并执行。

## 预测归档与数据盘点

- 统计型预测参考的固定落盘见 `.cursor/rules/lottery-prediction-storage.mdc`（`history/*_prediction.md`）。
- 总控做文件盘点时，**优先**在仓库根执行 `python src/scripts/lottery.py inventory`，以 JSON 输出为清单依据；亦可配合 IDE 文件树核对路径。

## 组号与预测的边界

- **`lottery-combo-optimize`** 只产出投注方案与注数金额；**不得**重新推导冷热/常见号等统计结论。若方案需对齐某次预测，必须显式引用 `history/*_prediction.md`（排列5 为 `history/pailie5_prediction.md`）或用户粘贴的预测摘要作为**输入约束**，而非在组号对话里重做预测。
- **排列5 组号**：以 **单式多注** 满足默认 **10～30 元/期** 为主；**复式/组选/倍投**须按官方玩法写清注数公式（直选复式为各位候选个数之积），详见 **`lottery-combo-optimize`** 技能，不得套用大乐透/双色球的球区复式叙事。
- **投注推荐金额带（仓库默认）**：统计规律输出完成后，面向打票的推荐方案须至少一套 **合计 10～30 元（含端点）/ 期**；与 `AGENTS.md`、`src/lottery/config.py` 中 `DEFAULT_COMBO_BUDGET_MIN_YUAN` / `DEFAULT_COMBO_BUDGET_MAX_YUAN` 及预测文末附录一致。用户另有声明预算时从其声明。
- **单式优选强制输出**：凡预测输出涉及“明确号码”时，必须再单列 **1 注分数最高的单式**；该注按同口径评分排序后**直接取各球区前 N 个**组成（大乐透前 5+后 2，双色球红 6+蓝 1，快乐八候选取前 11 用于选十 11 码复式），并给出总分、关键因子与生成时间。须保留随机性声明，不得表述为必中。
- **快乐八去重叠上限（新增硬规则）**：快乐八预测中的“参考开奖 20 码”与最新一期真实开奖 20 码的重合数必须 **≤ 6**。若按原评分候选得到的重合数 > 6，必须通过降权/替换（优先替换重合边界低分号）将重合数压到 6 及以下，并在输出中注明已执行该约束。
