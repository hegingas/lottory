---
name: 选双色球
description: 双色球三位一体选号——历史分析+号码预测+投注组号一条龙。当用户说"选双色球"、"双色球选号"、"双色球预测"、"双色球推荐号码"、"帮我选双色球"、"双色球买什么"时使用。覆盖频次遗漏冷热分析、区间掩码马尔可夫+多因子加权出号、10~30元投注方案。
---

# 双色球选号（分析 → 预测 → 组号 三位一体）

## 原则

- 彩票为随机游戏，历史统计**不能**保证未来结果。输出必须声明：娱乐目的、概率本质、过往统计仅供参考。
- **去核心化约束（强制）**：禁止直接围绕最新窗口 Top 热号骨架拼注，必须加入反集中约束（分区均衡、过热惩罚、热温冷混合），在正文写明"已执行去核心化约束"。
- **防重合约束（强制）**：任一预测单式不得与历史任一期完全重合；与最新一期重合号数 ≤3（全号码位 7 码计算）。若不满足，替换低分重合号后再输出。
- **时间戳规则（强制）**：`最后更新`、`预测生成时间` 使用北京时间 ISO-8601（`+08:00`）。

## 数据来源

1. `data/processed/ssq_draws.csv`（优先）
2. 否则回退 `data/raw/` 或用户指定路径，并在输出中写明数据来源层级

## 执行流程（三步走，不可跳步）

### 第一步：历史分析

基于 `data/processed/ssq_draws.csv`，默认近 **30** 期（`DEFAULT_STATS_WINDOW`），输出：

- **频次统计**：红球（01–33）和蓝球（01–16）各号码出现次数排名
- **冷热分级**：热号（Top 红球 8 / 蓝球 4）、温号、冷号（Bottom 红球 8 / 蓝球 4）
- **遗漏分析**：当前遗漏期数、最大历史遗漏、是否处于冷态
- **结构分析**：奇偶比、大小比（红球 01–16 为小，17–33 为大）、和值区间、AC 值（红球 6 枚，AC = D − 5）、连号个数、质合比
- **多窗口对照**（可选）：30/100/300 期间一指标对比表，仅作稳定性描述

完成后**必须**更新 `history/shuangseqiu_analysis.md`。

### 第二步：号码预测

**规则口径**：

- **区间掩码马尔可夫**（定候选球域）：红球 7 段（01–05 / 06–10 / 11–15 / 16–20 / 21–25 / 26–30 / 31–33，每段约 5 个连续号，末段 31–33），蓝球 4 段（01–04 / 05–08 / 09–12 / 13–16，每段 4 个号）。计算每段「是否至少出 1 球」的二进制掩码，全历史做相邻期一阶马尔可夫 + 拉普拉斯平滑，取末态条件下概率最大的下一掩码，扩展至可凑满所需球数。**仅在掩码并集内**做后续选号。
- **多因子加权**（在候选域内打分）：min-max 归一后按 8 项独立因子加权：

| 因子 | 权重 |
|---|---|
| 马尔可夫转移（一阶+二阶混合，40%/60%） | 25% |
| 当前遗漏 | 18% |
| 频次（窗口内出现次数） | 14% |
| 区间热度 | 12% |
| 近 K 期密度 | 10% |
| 奇偶对齐 | 8% |
| 大小/半区对齐 | 8% |
| 和值带对齐 | 5% |

- **分区上限**：红球每 5 码一小区至多 **2** 个；蓝球每 4 码一小区至多 **2** 个（蓝仅 1 码时自然满足）。
- **自动择优**（可选）：`use_mask=False` 跳过掩码约束，全号池开放；`--auto-strategy` 自动选取更优策略。

**区间选择说明（强制）**：必须解释：上期红/蓝各区命中掩码（人类可读）→ 条件最大概率下一掩码（含平滑概率值）→ 历史转移总次数 → 展开后最终使用的掩码 → 边缘情况说明。

**强制输出**：

- **5 注单式**（每注红球 6 + 蓝球 1）
- 每注逐号选择原因 + 预测生成时间（北京时间 ISO-8601）
- **1 注单式优选**：分数最高的单式，含总分、关键因子贡献
- 标注"防重合约束已执行（历史不全重合 + 最新期≤3）"

完成后**必须**更新 `history/shuangseqiu_prediction.md`。

### 第三步：投注组号

基于预测结果，在 **10～30 元（含）** 金额带内构造投注方案：

- 覆盖单式、复式、胆拖的注数与金额计算
- 用户声明不要倍投时，仅用多注单式/复式/胆拖凑额，不得用倍数投注
- 红球复式注数 = C(n,6)，蓝球复式注数 = m，总注数 = 红球注数 × 蓝球个数
- 输出注数金额明细表

---

## 输出模板

```markdown
## 口径说明
- 彩种：双色球
- 期号范围：<min> ~ <max>
- 统计窗口：近 30 期
- 指标定义：…

## 历史分析摘要
（热号/冷号/遗漏/结构）

## 区间掩码说明
（上期红/蓝掩码 → 下一掩码 → 扩展掩码）

## 明确号码输出（强制）
### 5 注单式
| 注号 | 红球 | 蓝球 | 选择原因 |
|---|---|---|---|
| 1 | … | … | … |
| … | … | … | … |

### 单式优选
- 号码：红球 … + 蓝球 …
- 总分：…
- 关键因子：…
- 生成时间：…

## 使用说明
以上仅为历史统计参考，下一期开奖仍为独立随机事件。请理性购彩，量力而行。

## 附录：投注推荐（10～30 元）
| 方案 | 类型 | 号码 | 注数 | 金额 |
|---|---|---|---|---|
| … | … | … | … | … |
```

## 落盘约定

| 文件 | 对应步骤 |
|---|---|
| `history/shuangseqiu_analysis.md` | 历史分析 |
| `history/shuangseqiu_prediction.md` | 号码预测 |

每次执行后必须更新对应文件。元数据含：最后更新（ISO 8601）、期号范围、所用数据路径。

## 职责隔离

本 Skill 仅服务双色球选号。**禁止**：其他彩种预测（分别用「选大乐透」「选快乐八」「选排列5」「选七星彩」）、数据采集落盘（用 `lottery-draw-sync` / `lottery-draw-dlt-ssq`）、全流程编排（用 `lottery-manager`）。

## 禁止

- 不得输出「稳赚」「必中」「投资回报率」等误导表述
- 不得输出「建议投入金额占收入比例」类财务建议
- 不得伪造「模型回测收益率」
- 不得在未经用户要求下创建或修改脚本文件
