# 七星彩 — 历史数据分析归档

> **最后更新**：2026-05-14T22:51:33+08:00
> **统计窗口（默认）**：近 **30** 期，期号 **`26024`–`26053`**（期末尾连续段，至多 **30** 期）。
> **全表收录**：`data/processed/qxc_draws.csv` 共 **841** 期，期号 **`20100`–`26053`**（溯源见 `data/processed/manifest.json`）

---

## 摘要（数据范围与口径）

本次基于 `data/processed/qxc_draws.csv` 进行描述性统计。每期包含 **前区 6 位**（`d1`–`d6`，0–9，允许重复）+ **后区 1 位**（`special`，0–14）。

## 结果摘要

- 前区 6 位数字热度 Top5：`9`（24次）、`2`（22次）、`8`（21次）、`3`（20次）、`1`（19次）
- 前区 6 位数字冷度 Top5：`4`（12次）、`0`（13次）、`7`（15次）、`6`（16次）、`5`（18次）
- 后区数字热度 Top5：`13`（6次）、`12`（5次）、`14`（4次）、`1`（2次）、`5`（2次）
- 后区数字冷度 Top5：`0`（1次）、`2`（1次）、`4`（1次）、`7`（1次）、`9`（1次）
- 前区含重复数字的期数占比：**25/30 = 83.33%**
- 七星和值（前区+后区）：均值 37.47，中位数 37，Q1–Q3 约 32–45
- 前区跨度（max−min）：均值 7.03，中位数 8，Q1–Q3 约 6–8

## 分位热度（Top3）

- 前区第1位：`2`（5次）、`1`（4次）、`6`（4次）
- 前区第2位：`9`（6次）、`8`（5次）、`4`（4次）
- 前区第3位：`8`（5次）、`9`（5次）、`2`（4次）
- 前区第4位：`1`（5次）、`2`（5次）、`9`（5次）
- 前区第5位：`5`（8次）、`1`（3次）、`2`（3次）
- 前区第6位：`3`（7次）、`0`（4次）、`2`（4次）
- 后区（special 0–14）：`13`（6次）、`12`（5次）、`14`（4次）

## 局限

七星彩开奖结果具有随机性；以上统计仅用于历史描述，不构成中奖承诺或投资建议。
